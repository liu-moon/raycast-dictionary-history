# Raycast 查词历史记录脚本 — 需求文档

## 1. 背景与问题

- Raycast 内置的 `Define Word`（词典查询）命令，其历史记录存储在**加密数据库** `raycast-enc.sqlite` 中
- 官方 **Export Settings & Data** 功能不包含词典查询历史（已核实导出选项列表，无 Dictionary History 项）
- 无解密密钥，无法通过 SQLCipher 等工具读取历史数据
- **结论：过去查过的词无法找回，只能从现在开始主动记录**

## 2. 核心思路

Raycast Script Command **不支持监听/hook 其他命令**，无法"寄生"在 `Define Word` 上自动记录。
因此方案改为：**编写一个新脚本，功能上替代 `Define Word`**，查词的同时自动写入本地文件。

以后查词习惯：从用 `Define Word` → 改为触发这个新脚本，效果类似，但会顺手记录历史。

## 3. 功能需求

### 3.1 核心功能

| 功能 | 说明 |
|---|---|
| 查词 | 输入单词，返回释义（可调用词典 API 或系统 `dict`/`open dict://` 命令） |
| 自动记录 | 每次查词后，自动将「单词、释义、时间」追加写入本地历史文件 |
| 去重（可选） | 同一单词多次查询时，可选择更新查询次数/最后查询时间，而非重复记录 |
| 导出友好 | 历史文件格式选用 CSV 或 Markdown，随时可直接打开/导入 Excel/Anki |

### 3.2 触发方式

- **手动输入模式**
  在 Raycast 中输入脚本名，再传入要查的单词

### 3.3 输出/交互需求

- 查完词后需要在 Raycast 界面**弹出释义结果**（不能只是静默写文件，否则失去查词的即时反馈）
- 可选：查询失败时（单词查不到）也要有明确提示，避免误记录空数据

## 4. 数据存储设计

### 4.1 存储位置

建议固定保存在一个明确路径

### 4.2 字段设计（CSV 示例）

| 字段名 | 说明 | 示例 |
|---|---|---|
| word | 查询的单词 | `ambiguous` |
| definition | 释义（可截取简要版本） | `open to more than one interpretation` |
| query_time | 查询时间戳 | `2026-09-02 10:48:00` |
| query_count | 该单词累计查询次数（可选） | `1` |

### 4.3 文件初始化逻辑

- 首次运行时，若目标文件不存在，需自动创建并写入表头（header）
- 后续每次运行，以**追加模式**写入新行，避免覆盖历史数据

## 5. 技术实现建议

### 5.1 脚本语言

- 推荐 **Python**（内置 `csv`、`datetime`、`urllib`/`requests` 模块，编写维护成本低）
- Raycast Script Command 需要在脚本开头加规范注释头，声明标题、模式、图标等元信息

### 5.2 查词数据来源

- **调用 macOS 系统词典**

## 6. 非功能需求

- **编码问题**：CSV 写入时注意 UTF-8 编码，避免中文/特殊字符乱码
- **幂等性**：多次运行脚本不应破坏已有历史文件结构

## 7. 后续可扩展功能（非本次必须）

- 支持导出为 Anki 记忆卡片格式（`.apkg` 或 CSV 兼容格式）
- 支持按日期/词频生成统计报表（如"本周查了多少新词"）

## 8. 验收标准

- [ ] 在 Raycast 中输入脚本名，能正常查到单词释义并展示
- [ ] 查词后，本地历史文件里新增一行记录，字段完整
- [ ] 重复运行多次，历史文件不会丢失之前的记录（追加而非覆盖）
- [ ] 查询一个不存在的单词，脚本不报错崩溃，且不写入无效记录
- [ ] 历史文件可以直接用 Excel / Numbers 打开，格式正常无乱码
